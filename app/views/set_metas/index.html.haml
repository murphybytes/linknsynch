%h2 Samples 
%table.table.table-bordered
  %tr
    %th 
      Sample Name
    %th 
      Description
  - @sets.each do |set|
    %tr
      %td= link_to set.name, set_meta_path( set.id ) 
      %td= set.description
